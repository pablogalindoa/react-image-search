import React from "react";
import ReaactDOM from "react-dom/client";
import App from "./App";

const el = document.getElementById("root");
const root = ReaactDOM.createRoot(el);

root.render(<App />);
